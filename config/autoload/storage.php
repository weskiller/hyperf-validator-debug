<?php
    return [
        // 必须加 / 前缀 !!
        #'root' => '/data/resource/resource', //线上
        'root' => '/data/resource',
        // 必须加 / 前缀 !!
        'prefix' => '/resource',
        // 必须加 / 前缀 !!
        'tmp' => '/tmp/' . env('APP_NAME'),
    ];